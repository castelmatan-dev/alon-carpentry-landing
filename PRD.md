---
type: work
title: PRD — עמוד נחיתה לנגרות אלון ברששת
description: אפיון מוצר למטלת בית 7; מאושר על ידי מתן 2.9.2026
updated: 2026-09-02
status: מאושר — בבנייה
---

# PRD — עמוד נחיתה: נגרות אלון ברששת

**גרסה:** 1.0 (מאושר — 2.9.2026) · **תאריך:** 2.9.2026 · **מאפיין:** מתן + קלוד · **לקוח:** אלון ברששת (פיקטיבי, מטלת קורס)
**מקור יחיד לדרישות:** המייל של הלקוח מ-2.9.2026. כל מה שאינו במייל מסומן 🔶 ודורש אישור.

---

## 1. הבעיה

אלון נגר עצמאי בפרדס חנה מ-2017, עובד לבד, בהזמנה אישית. אין לו שום נוכחות דיגיטלית.
מה שקורה היום: לקוח פוטנציאלי שומע עליו מחבר → מבקש לראות עבודות → אלון שולח תמונות בוואטסאפ אחת-אחת → הפנייה מתערבבת בין הצ'אטים → הוא מגלה אותה אחרי שבוע, או לא בכלל.

**שתי עלויות נפרדות, ושתיהן צריכות פתרון:**

| הכאב | מה זה עולה לו | מה פותר |
|---|---|---|
| נראה חובבני, אין מה להראות | מפסיד עבודות לפני שדיברו | עמוד שנראה מקצועי ומסביר את התהליך |
| פניות נבלעות בוואטסאפ | מפסיד עבודות אחרי שדיברו | ערוץ קליטה אחד, מסודר, שלא מתערבב |

**התובנה המנחה:** אלון לא ביקש "אתר". הוא ביקש להפסיק לפספס עבודות. כל החלטה במסמך הזה נמדדת מול זה.

---

## 2. מטרה ומדדי הצלחה

**מטרת-על:** לקוח שהגיע לעמוד מהטלפון, באמצע יום עבודה, ברשת גרועה — מבין מי אלון ומה התהליך, ומשאיר פנייה מסודרת שמגיעה אליו למקום אחד.

| מדד | יעד | איך נבדק |
|---|---|---|
| טעינה ראשונה בנייד | LCP < 2.5 שניות ב-Slow 4G, מכשיר בינוני | throttling ב-DevTools, שלב הבדיקות |
| משקל מעל הקיפול | < 150KB (HTML+CSS+גופן+תמונת hero) | Network panel |
| פניות אבודות | 0 — כל שליחה נגמרת באישור ברור או במסלול גיבוי גלוי | בדיקת שני המסלולים ידנית |
| שדות בטופס | 5, לא יותר | ספירה |
| טענות לא מאומתות בעמוד | 0 | מול מלאי העובדות (§6) |

**מה לא נמדד בגרסה 1:** אנליטיקס. אין. מוסיפים רק אם אלון יבקש — אחרת זה משקל ומורכבות בלי שאלה שהם עונים עליה.

---

## 3. משתמשים

**המשתמש העיקרי — לקוח פוטנציאלי.** הגיע מהמלצה של חבר, לא מחיפוש. כבר סומך על אלון במידה מסוימת; מה שהוא צריך זה אישור שזה עסק אמיתי, מושג על הסגנון, והבנה כמה זמן זה לוקח. נייד, רשת לא יציבה, סבלנות נמוכה. הוא לא משווה ספקים — הוא מחליט אם להרים טלפון.

**המשתמש השני — אלון.** מקבל את התוצר, לא נכנס לעמוד. הצלחה מבחינתו: הפניות מגיעות למקום אחד קבוע, בפורמט קבוע, ואפשר להעביר לינק בוואטסאפ במקום שבע תמונות.

---

## 4. הכרעות מוצר (נחתמו בסשן האפיון)

| # | הכרעה | מה נבחר | נימוק |
|---|---|---|---|
| 1 | מסלול הפנייה | **n8n webhook** | הפנייה נכנסת ל-workflow: מייל מסודר + תיעוד ב-executions. נותן לאלון "מקום אחד" אמיתי, ותואם את מפגש 7 |
| 2 | מבנה | **עמוד יחיד גולל** | טעינה אחת, אפס ניווט לאיבוד בנייד; `index.html` בשורש כפי ש-Vercel מצפה |
| 3 | תמונות | **הדמיות AI עם תווית "הדמיה" גלויה** | אלון ביקש לא להמציא. הדמיה מסומנת מעבירה סגנון בלי לטעון טענה שקרית |
| 4 | לוגו | **Wordmark טיפוגרפי** | טקסט, לא קובץ — נטען מיידית ולא נראה כמו קליפארט |
| 5 | ערוצי קשר | **הטופס הוא ה-CTA היחיד** | הטלפון מופיע בעמוד כפרט קשר עובדתי — לא ככפתור, לא כהנעה לפעולה, לא בצבע הדגשה. מי שמחפש אותו ימצא; אף אחד לא מובל אליו |
| 6 | שדות טופס | **6** | שם · טלפון · **מייל** · יישוב · סוג רהיט · מה צריך (ר' תיקון ב-§7) |
| 7 | כשל בשליחה | **נפילה ל-mailto עם הפרטים** | הלקוח לא מאבד את מה שכתב, והפנייה לא נעלמת |
| 8 | פריסה | **Git מקומי + GitHub על ידי קלוד; Vercel ידנית על ידי מתן** | הנחיית המטלה: לא מבקשים מקלוד קוד לפרוס. מתן מייבא ומחזיר את הכתובת |
| 9 | יעד הפניות | **devai.test5858@gmail.com** | תיבת הקורס. מתן עומד במקום אלון לצורך המטלה |
| 10 | ייצור התמונות | **Cloudinary MCP (מריץ את Gemini/GPT-Image ישירות)** | חוסך הורדה והעלאה ידנית, ומאפשר עקביות בין התמונות דרך image-to-image — ר' §8 |

---

## 5. מבנה העמוד

סדר הסקשנים, מלמעלה למטה. כל סקשן מצדיק את מקומו במשפט אחד.

1. **כותרת עליונה** — wordmark + קישור עוגן לטופס. דביק? לא — חוסך JS ומקום מסך.
2. **Hero** — מי, מה, איפה, ומאיזו שנה. CTA ראשי לטופס. *נותן ללקוח את שלושת הדברים שהוא צריך ב-3 שניות.*
3. **מה אני מייצר** — שלוש קטגוריות: שולחנות אוכל וסלון · ארונות ומזנונים · ספריות וקירות אחסון. *עונה על "האם הוא עושה את מה שאני צריך".*
4. **איך זה עובד** — חמשת השלבים של אלון. *הנכס החזק ביותר במייל: עונה על "כמה זמן" ו"מה קורה" בלי לנקוב במחיר.*
5. **הדמיות** — 6–8 תמונות מסומנות + משפט הסבר גלוי. *מעביר סגנון ורמת גימור.*
6. **פרטים** — עצים ויבוש, אחריות, הרכבה כלולה, תמחור לגופו, ביקור בתיאום. *מסיר את שאלות הבירור הנפוצות.*
7. **טופס** — 5 שדות. *לב המוצר.*
8. **פוטר** — שם, בית המלאכה, טלפון, מייל, "ביקור בתיאום מראש".

**מחוץ לגבולות (Non-goals) — גרסה 1:**
המלצות · ביקורות · פרסים · דירוגים · סופרלטיבים · מחירון · וואטסאפ · בלוג · ריבוי עמודים · CMS · צ'אט · ניוזלטר · קוקיז · אנליטיקס · תמונות אמיתיות (יוחלפו כשיהיו).

---

## 6. מלאי עובדות — מקור יחיד לכל טקסט בעמוד

**כלל מחייב:** כל משפט בעמוד מבוסס על שורה מהטבלה הזאת. אין הרחבה, אין ניסוח שיווקי שמוסיף טענה, אין מספרים חדשים. מה שלא כאן — לא נכתב.

| # | עובדה | מקור |
|---|---|---|
| 1 | נגר, עובד לבד | המייל |
| 2 | פרדס חנה, מ-2017 | המייל |
| 3 | רהיטים בהזמנה אישית, לפי מידות הלקוח | המייל |
| 4 | אין חנות; בית מלאכה ברחוב הבנים | המייל |
| 5 | מקבל לקוחות בתיאום מראש | המייל |
| 6 | שולחנות אוכל וסלון · ארונות ומזנונים · ספריות וקירות אחסון | המייל |
| 7 | תהליך: שיחה → מדידה בבית → סקיצה והצעת מחיר → ייצור → הרכבה | המייל |
| 8 | ייצור: 3–6 שבועות אחרי אישור | המייל |
| 9 | הרכבה במקום, כלולה במחיר | המייל |
| 10 | עצים: אלון, אגוז, אורן | המייל |
| 11 | העץ מיובש בתנור לפני העבודה | המייל |
| 12 | אחריות שנתיים על העבודה | המייל |
| 13 | אין מחירון; כל עבודה מתומחרת לגופה | המייל |
| 14 | 054-000-0000 · alon@example.co.il | המייל |

**איסורים מפורשים של הלקוח:** אין "הכי טוב בארץ" ואין ניסוח דומה · אין פרסים · אין המלצות כתובות · אין המצאות.

**סגנון הכתיבה:** גוף ראשון, בגובה העיניים, משפטים קצרים. אלון כתב עובדתית ובלי התלהמות — הטקסט בעמוד נשמע כמוהו. תיאור ולא שכנוע.

---

## 7. הטופס — המפרט המלא

### שדות (6)

| שדה | סוג | חובה | הערה |
|---|---|---|---|
| שם | text | כן | |
| טלפון | tel | כן | `inputmode="tel"` — מקלדת מספרים בנייד |
| מייל | email | כן | **הכתובת שאליה נשלח מייל האישור.** בלעדיה אין מה לשלוח |
| יישוב | text | כן | אלון מגיע למדוד בבית; מרחק קובע רלוונטיות |
| סוג רהיט | select | לא | שלוש הקטגוריות + "עוד לא בטוח" כברירת מחדל |
| מה צריך | textarea | כן | חופשי, 3 שורות |

**תיקון לאפיון שאושר.** ה-PRD נחתם על 5 שדות. הנחיות המטלה מחייבות מייל אישור לפונה — ומייל אישור מחייב כתובת מייל. זה אילוץ, לא שיקול דעת. כדי לא להעמיס בשישה שדות חובה, "סוג רהיט" ירד לרשות עם ברירת מחדל, כך שמספר שדות החובה נשאר חמישה.

תוספת נסתרת: שדה honeypot נגד בוטים. אין CAPTCHA — חיכוך מיותר בעסק בנפח הזה.

### מצבים
- **התחלה** — כפתור פעיל, בלי הודעות.
- **שליחה** — הכפתור נעול עם חיווי; מונע לחיצה כפולה ברשת איטית.
- **הצלחה** — הטופס מוחלף בהודעה שאומרת שהפנייה התקבלה, **ושנשלח מייל אישור לכתובת שהוזנה**. אין הבטחת זמן תגובה — אלון לא מסר אחד.
- **כשל** — הודעה ברורה + כפתור "שלח במייל" שפותח `mailto:` עם כל מה שכבר מולא בגוף ההודעה, והטלפון מוצג לצדו. **הערכים לא נמחקים מהשדות.**

### החוזה מול n8n

`POST` בגוף JSON לכתובת ה-Production של ה-webhook. שדות: `name`, `phone`, `email`, `city`, `category`, `details`.
הצלחה = 200. כל דבר אחר, כולל timeout, נחשב כשל ומפעיל את מסלול הגיבוי.

**ה-workflow מבצע שלוש פעולות:**

| # | פעולה | יעד | תוכן |
|---|---|---|---|
| 1 | שורה בגוגל שיטס | גיליון ייעודי | כל שישה השדות + חותמת זמן |
| 2 | מייל התראה לבעל העסק | `devai.test5858@gmail.com` | טקסט פשוט, ברור וקריא. לא מעוצב — נועד להיקרא מהר בטלפון |
| 3 | מייל אישור לפונה | **הכתובת שהוזנה בטופס** | מעוצב ב-HTML. ר' פירוט למטה |

**תנאים תפעוליים:**
- ה-workflow חייב להיות ב-**publish**. במצב test אין האזנה קבועה — כל פנייה תיפול.
- **CORS:** בזמן הפיתוח `Allowed Origins` נשאר `*` כדי שנוכל לבדוק מהמחשב. **נסגר לדומיין ה-Vercel בלבד בשלב האחרון**, אחרי שמתן מחזיר את הכתובת.
- **מגבלת אמת:** באתר סטטי כתובת ה-webhook יושבת בקוד צד-הלקוח וגלויה ב-F12. אין דרך להסתיר אותה. ההגנה היא CORS מצומצם + ולידציה בצד n8n — **לא** סודיות. הגנה חזקה יותר = שינוי ארכיטקטורה (פונקציית שרת), לא שינוי בעמוד.

### מייל האישור לפונה

- מעוצב ב-HTML, באותה שפה עיצובית של העמוד — אותם צבעים, אותה טיפוגרפיה.
- **הלוגו והתמונות משובצים מ-Cloudinary.** הם כבר יושבים על כתובת אינטרנט ציבורית, ולכן משתבצים במייל בלי קבצים מצורפים. זו הסיבה שהלוגו נשמר גם כתמונה ב-Cloudinary ולא רק כטקסט בעמוד.
- תוכן: תודה · מה נמסר (החזרת פרטי הפנייה) · מה קורה עכשיו (השלב הבא בתהליך של אלון) · פרטי קשר.
- טבלאות ו-CSS inline — לקוחות מייל לא תומכים ב-CSS מודרני. זה לא אותו קוד כמו העמוד.

## 8. תמונות

**מה המטלה מנחה:** לייצר ב-ChatGPT או Gemini, להוריד למחשב, ולהעלות ל-Cloudinary. לתאר את החומר, התאורה והזווית — ולא את הריהוט.

### ההמלצה: לוותר על ההורדה וההעלאה הידנית

ה-Cloudinary MCP שמחובר לסשן הזה מריץ בעצמו **את אותן משפחות מודלים** שהמטלה מפנה אליהן:

| משפחה במטלה | מה זה ב-Cloudinary MCP |
|---|---|
| Gemini | `nano-banana` → `nano-banana-2` (ברירת המחדל של הכלי) |
| ChatGPT | `gpt-image` → `gpt-image-2` |
| — | בנוסף: `flux-2-pro` (פוטוריאליזם), `ideogram-v4`, `recraft-v4` |

כלומר שלושת השלבים (ייצור → הורדה → העלאה) מתכווצים לקריאה אחת, והתמונה נשמרת ישירות כנכס מנוהל בחשבון. אפס קבצים שמסתובבים במחשב, אפס העלאה ידנית, ושמות ה-`public_id` נקבעים על ידינו מראש.

### הנימוק המכריע — עקביות בין התמונות

6–8 תמונות צריכות להיראות כמו התוצרת של **בית מלאכה אחד**, לא כמו שמונה תמונות סטוק שאין ביניהן קשר. לקוח מזהה חוסר עקביות מיד, גם בלי לדעת להסביר למה.

המנגנון שנותן את זה: `generate-image-from-images` עם מודל edit (למשל `nano-banana-2-edit`) מקבל את **התמונה הראשונה שאושרה כ-reference**, וכל שאר התמונות נגזרות ממנה — אותה פלטת עץ, אותה תאורה, אותו אופי חלל. בעבודה ידנית מול ממשק הצ'אט אין דרך אמינה לשחזר את זה על פני שמונה ייצורים נפרדים.

בנוסף: `seed` קבוע (במודלים שתומכים) = אפשר לשחזר ייצור במדויק במקום להתחיל מאפס.

### תוכנית העבודה

1. **מבחן השוואה על תמונה אחת.** אותו פרומפט בדיוק ב-`nano-banana-2` (Gemini) · `gpt-image-2` (ChatGPT) · `flux-2-pro`. מתן בוחר זוכה בעיניים. זול, מהיר, וחוסך גילוי מאוחר שהמודל לא מתאים.
2. **תמונת ה-hero** מיוצרת במודל הזוכה ומאושרת.
3. **שאר התמונות** ב-image-to-image עם ה-hero כ-reference.
4. פסילה וייצור מחדש של כל תמונה שלא עומדת ברף.

🔒 **שער אישור — כתיבת הפרומפטים.** הפרומפטים לתמונות ייכתבו בשלב הבנייה עם סקיל `frontend-design` (כובע מעצב: חומר, תאורה, זווית, פלטה, אופי חלל — לא "רהיט יפה"), ויוגשו למתן כסיכום מרוכז **לאישור לפני שמייצרים משהו**. אין ייצור לפני אישור.

### תוכן התמונות
6–8 סה"כ: אחת ל-hero · 2–3 לכל קטגוריה (שולחנות · ארונות ומזנונים · ספריות וקירות אחסון).
עצים מהמלאי בלבד — אלון, אגוז, אורן. בלי לוגואים, בלי אנשים, בלי טקסט בתוך התמונה, בלי מותגים מזוהים.

### תווית חובה — עודכן 2.9.2026
**כל תמונה נושאת את המילה "הדמיה" בכיתוב שלה**, בטקסט קריא ובגודל התווית של העמוד.

עודכן לאחר סקירת מתן: הניסוח המקורי דרש הבלטה, והתוצאה הייתה שהגילוי הנאות הפך לדבר
הבולט ביותר בסקשן. הפסקה שהסבירה את ההדמיות ירדה, והתגיות השחורות הוחלפו בטקסט שקט
בתוך הכיתוב. **מה שלא השתנה: התווית קיימת על כל תמונה בלי יוצא מן הכלל.** הבקשה
המפורשת של אלון לא להמציא חלה גם על תמונות, ולכן "שקט" הוא שינוי לגיטימי ו"לא קיים" אינו.

### מסירה — כאן נסגר סעיף 6 ב"שימו לב"

התמונה **לא** תרד לתיקיית `images/`. היא נמסרת מ-Cloudinary דרך URL עם טרנספורמציות, וזה מה שפותר את "התמונה כבדה מדי":

- `f_auto` — הדפדפן מקבל AVIF/WebP לפי מה שהוא תומך בו, בלי שנחליט מראש.
- `q_auto` — דחיסה לפי תוכן התמונה, לא איכות קבועה.
- `w_` + `dpr_auto` — הרוחב מותאם לגודל המסך ולצפיפות הפיקסלים. טלפון לא מוריד תמונת דסקטופ.
- `c_fill,ar_4:3,g_auto` — יחס וחיתוך אחידים לכל הגלריה.

מבנה: `.../upload/f_auto,q_auto,c_fill,ar_4:3,g_auto,w_600/alon-carpentry/render-dining-01`
ב-HTML: `srcset` עם 3–4 רוחבים + `sizes` + `width`/`height` מפורשים + `loading="lazy"` לכל מה שמתחת לקיפול. תמונת ה-hero **לא** lazy.

### החלפה בצילומים אמיתיים
`public_id` בתבנית `alon-carpentry/render-<קטגוריה>-<מספר>`. כשיגיעו צילומים — מעלים תחת `photo-` באותו מבנה, מחליפים מחרוזת אחת ב-HTML, ומסירים את התווית. ה-HTML לא נוגע בלוגיקה.

## 9. עיצוב

הכיוון העיצובי ייקבע בשלב הבנייה עם סקיל **`frontend-design`**. אילוצים מחייבים:

- **עברית, RTL מלא** — `dir="rtl"`, כולל טופס וסדר אייקונים.
- **גופן מערכת בלבד.** גופן עברי מהרשת שוקל 100KB+ וחוסם רינדור. לא משתלם מול יעד הטעינה.
- **`:root` כשורש עיצובי** — צבעים, מרווחים, רדיוסים, טיפוגרפיה כמשתני CSS במקום אחד. (מפגש 7; מונע סחיפה עיצובית בעריכות עתידיות עם AI.)
- **נייד ראשון.** נבנה ל-390px ומתרחב, לא להפך.
- **אפס JS מלבד הטופס.** בלי ספריות, בלי framework, בלי אנימציות שדורשות סקריפט.
- **נגישות:** ניגודיות AA · שטחי נגיעה ≥ 44px · `label` לכל שדה · פוקוס נראה · העמוד קריא ושמיש גם אם התמונות לא נטענו.
- שלושה קבצים: `index.html` · `style.css` · `images/` — בשורש ה-repo.

---

## 10. בדיקות (סקיל `webapp-testing`)

לא נמסר עד שכל אלה עברו:

1. מסך 390px — אין גלילה אופקית, אין טקסט חתוך, אין כפתור צפוף.
2. **הטופס נשלח בפועל ומגיע ליעד ב-n8n.** לא "נראה שנשלח" — נבדק בקצה השני.
3. **מסלול הכשל נבדק בכוונה** (webhook מכובה): ההודעה מופיעה, `mailto` נפתח עם הנתונים, השדות לא התרוקנו.
4. ולידציה: שליחה ריקה, טלפון לא תקין — הודעות בעברית, ליד השדה.
5. Slow 4G — העמוד שמיש לפני שהתמונות ירדו.
6. **משקל התמונות בפועל** — בדיקה ב-Network שהדפדפן מקבל AVIF/WebP ולא PNG, ושתמונת גלריה בנייד < 100KB.
7. אפס שגיאות בקונסול.
8. מעבר טקסט מול §6 — כל משפט מוצלב מול מלאי העובדות.
9. צילומי מסך נייד + דסקטופ מצורפים למסירה.

---

## 11. גרסאות ופריסה

**Git — מהרגע הראשון, לא בסוף.**
- `git init` בתיקיית `homework-7/`. `index.html` יושב בשורש התיקייה הזאת, כפי ש-Vercel מצפה.
- **commit אחרי כל שינוי משמעותי**, לא בסוף העבודה. הודעה בעברית שמסבירה מה השתנה, לא "עדכונים".
- commit = שינוי קוהרנטי אחד.
- `.gitignore` מותאם לפרויקט: `.env`, `.DS_Store`, לוגים, וכל ייצוא הרשאות מ-n8n.
- הערה: `course/` נמצאת ב-`.gitignore` של המאגר הלימודי, ולכן ה-repo הזה עצמאי ולא מתנגש איתו.

**חלוקת אחריות בפריסה:**

| שלב | מי |
|---|---|
| Git מקומי + commits | קלוד |
| repo פרטי ב-GitHub + push | קלוד |
| **Vercel: Add New → Import → Deploy** | **מתן** (הנחיית המטלה: לא מבקשים מקלוד לפרוס) |
| החזרת הכתובת החיה | מתן |
| סגירת CORS ב-n8n לדומיין | אחרי קבלת הכתובת |

## 12. פתוח 🔶

| # | שאלה | סטטוס |
|---|---|---|
| 1 | לאן נשלחת הפנייה | ✅ נסגר — `devai.test5858@gmail.com` |
| 2 | מפרסמים את כתובת בית המלאכה או רק "פרדס חנה"? | פתוח — הנחת עבודה: "פרדס חנה" בלבד, בלי מספר בית |
| 3 | תוך כמה זמן אלון חוזר לפונים? | פתוח — עד לתשובה, אין הבטחת זמן בשום מקום |
| 4 | עד לאיזה מרחק הוא מגיע למדוד? | פתוח — שדה היישוב נשאר פתוח, בלי סינון |
| 5 | הטלפון בעמוד | ✅ נסגר — מופיע כפרט קשר, לא כהנעה לפעולה |

## 13. מה במפורש לא בגרסה הזאת

תיק עבודות אמיתי (ממתין לצילומים) · דף פרויקט בודד · אנגלית · אנליטיקס · SEO מעבר לתגיות בסיסיות · יומן/תיאום תורים · תשלום · אזור לקוחות.

---

## 14. רשימת התוצרים (מתוך המטלה)

| ☐ | תוצר | הגדרה |
|---|---|---|
| ☐ | עמוד אחד ב-HTML ו-CSS נפרד | בלי ספריות חיצוניות, בלי כלי בנייה |
| ☐ | תמונה שיוצרה ב-AI ויושבת ב-Cloudinary | הועלתה לחשבון שלנו, הכתובת משובצת בקוד |
| ☐ | לוגו / שם עסק מעוצב | wordmark טיפוגרפי בעמוד + אותו עיצוב כתמונה ב-Cloudinary למייל |
| ☐ | טופס שמגיע ל-webhook ב-n8n | 6 שדות; המשתמש רואה הודעת הצלחה או שגיאה |
| ☐ | כל פנייה נשמרת בגוגל שיטס | שורה עם כל השדות וחותמת זמן |
| ☐ | מייל התראה לבעל העסק | ל-`devai.test5858@gmail.com`; פשוט וקריא |
| ☐ | מייל אישור לפונה, מעוצב ב-HTML | לכתובת שהוזנה בטופס |

---

## חתימה

מתן — אישור הטיוטה הזאת פותח את שלב הבנייה. שינוי בסעיפים 4, 6 או 7 אחרי החתימה הוא שינוי אפיון, לא תיקון.

**סטטוס:** ✅ **מאושר על ידי מתן — 2.9.2026.** אור ירוק לבנייה.

תיקונים שנכנסו אחרי החתימה ומסומנים במסמך: שדה מייל (אילוץ מהמטלה) · הטלפון גלוי בעמוד · יעד הפניות · הפריסה ל-Vercel בידי מתן.
